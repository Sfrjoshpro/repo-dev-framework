version: 1
task_id: V7-BLUEPRINT-SELF-HOSTING
checkpoint: validation-pending
branch: chore/v7-blueprint-self-hosting
commit: null

summary: >-
  Extended the self-hosted project blueprint to model the verified repository-health
  subsystem and its repository/context-health flow without changing executable health
  or task-guard behavior. Implementation, reference review, repository health, and diff
  scope review are complete. The explicit branch-base task-guard command remains pending
  because this chat's GitHub connector cannot execute arbitrary repository shell commands.

completed:
  - Added a verified repository-health subsystem node for repo_framework/health.py.
  - Connected repository health to active work, task contracts, the blueprint, and persisted-state requirements.
  - Updated the legacy repository-check node to represent its compatibility-entry-point role.
  - Added a verified repository/context-health logic flow and integrated health validation into bounded task delivery.
  - Reviewed blueprint node, edge, and flow references for valid IDs.
  - Reviewed the branch diff against V7 allowed_paths; no out-of-scope files were changed.
  - GitHub Actions repository health, compatibility health entry point, and standard task guard passed while V7 was validating.

validation:
  commands:
    - python scripts/task_guard.py --task tasks/V7-BLUEPRINT-SELF-HOSTING.yaml
    - python -m repo_framework check
    - python scripts/task_guard.py --task tasks/V7-BLUEPRINT-SELF-HOSTING.yaml --base-ref origin/main
    - review blueprint references for valid node, edge, and flow IDs
    - review final diff against allowed_paths
  passed:
    - User reported the task-specific pre-implementation guard returned task guard: OK.
    - GitHub Actions repository health passed on validation-state commit 2bdcaa20fc75b9eb29279f96696a17b6a431a894.
    - GitHub Actions compatibility repository check passed on the same validation-state commit.
    - GitHub Actions standard task guard passed on the same validation-state commit.
    - Branch comparison against main showed only V7 declared allowed paths.
    - Blueprint reference review found no missing node IDs in added edges or flow steps.
  pending:
    - python scripts/task_guard.py --task tasks/V7-BLUEPRINT-SELF-HOSTING.yaml --base-ref origin/main
  failed: []

open_unknowns: []
blocked_by:
  - Final task completion is gated only on the explicit branch-base task guard command.
next_action: >-
  Run python scripts/task_guard.py --task tasks/V7-BLUEPRINT-SELF-HOSTING.yaml --base-ref origin/main.
  If it returns task guard: OK, mark V7 done, clear ACTIVE_WORK to idle, update CURRENT_STATE
  to the completed V7 checkpoint, and open the V7 pull request. Do not begin V8 on this branch.

relevant_paths:
  - tasks/V7-BLUEPRINT-SELF-HOSTING.yaml
  - development-state/CURRENT_STATE.yaml
  - development-state/ACTIVE_WORK.yaml
  - development-state/blueprint/project-model.yaml
  - development-state/handoffs/V7-BLUEPRINT-SELF-HOSTING.yaml
notes:
  - No repository-health behavior, task-guard behavior, visualization UI, graph extraction, or roadmap expansion was introduced.
  - V7 remains validating rather than done until the explicit branch-base guard evidence exists.
