version: 1
project_phase: framework-development
status: ready

summary: >-
  V7 Blueprint Self-Hosting is merged to main and repository health passed on the merge
  commit. V8 Blueprint Visualization Contract is the active bounded milestone. Its purpose
  is to define visualization behavior from the existing project model before any UI stack
  or implementation is chosen.

current_milestone: V8-BLUEPRINT-VISUALIZATION-CONTRACT
active_task: tasks/V8-BLUEPRINT-VISUALIZATION-CONTRACT.yaml
blockers: []

next_valid_actions:
  - run the task-specific pre-implementation guard for V8
  - if the guard passes, implement only the visualization contract within allowed_paths
  - if a required decision or path falls outside the contract, reconcile the task before continuing
