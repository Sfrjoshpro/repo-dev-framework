version: 1
project_phase: framework-development
status: active

summary: >-
  V8 Blueprint Visualization Contract is merged. V9 Context Working Set is active to add
  persisted, machine-checkable working-context state so long-running agent sessions can
  minimize unnecessary context growth without relying only on agent discipline.

current_milestone: V9-CONTEXT-WORKING-SET
active_task: tasks/V9-CONTEXT-WORKING-SET.yaml
blockers: []

next_valid_actions:
  - run the V9 task-specific pre-implementation guard
  - implement only the V9 allowed paths after the guard passes
  - validate context working-set behavior before checkpoint and pull request
