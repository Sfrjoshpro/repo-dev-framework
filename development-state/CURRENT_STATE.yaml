version: 1
project_phase: framework-development
status: active

summary: >-
  V9 Context Working Set is merged on main. V10 Compute Market Routing Contract is active
  to persist the provider-agnostic design for current market data, verified-cost accounting,
  user-owned compute policy, routing, escalation, and benchmarking before provider integration.

current_milestone: V10-COMPUTE-MARKET-ROUTING-CONTRACT
active_task: tasks/V10-COMPUTE-MARKET-ROUTING-CONTRACT.yaml
blockers: []

next_valid_actions:
  - validate the active V10 task/context boundary
  - define only the V10 compute-market routing behavior contract
  - checkpoint and reconcile state before pull request
