# Project Model

The project model describes the system at a level useful for planning and navigation.

Track at least:

- major systems and responsibilities
- dependency direction
- important user flows
- important runtime or logic flows
- persistence boundaries
- external interfaces

The machine-readable companion lives at `development-state/blueprint/project-model.yaml`.

Do not try to model every function. Add detail when it improves planning, debugging, or ownership.
